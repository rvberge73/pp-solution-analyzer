version: '3.8'

services:
  pp-solution-analyzer:
    build: .
    image: pp-solution-analyzer:latest
    container_name: pp-solution-analyzer
    ports:
      - "8090:8090"
    restart: unless-stopped
